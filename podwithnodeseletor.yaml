apiVersion: v1

kind: Pod

metadata: 
  name: podwithnodeselector

spec:
  nodeselector:
    disktype: ssd
  containers:
  - name: container1
    image: yasirdocker/flag:latest
    ports:
    - containerPort: 80
